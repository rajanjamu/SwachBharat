<%= @clean.id %> | <%= @clean.user_id %> | <%= @clean.location %>
<%= image_tag(@clean.bphoto_url)%>
<%= image_tag(@clean.aphoto_url)%>


<%= link_to "Back", cleans_path %>
<%= link_to "Delete", clean_path(@clean), method: :delete %>